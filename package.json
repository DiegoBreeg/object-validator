{
  "name": "object-validator",
  "version": "1.0.0",
  "description": "Library to validate an object from object rules",
  "main": "./dist/index.js",
  "scripts": {
    "start": "clear && tsc && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["object validator", "validator", "object-validator"],
  "author": "DiegoBreeg",
  "license": "ISC"
}
